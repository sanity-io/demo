/** @alpha */
export const fontFamily = {
  mono: 'IBM Plex Mono, ui-monospace, SFMono-Regular, SF Mono, Menlo, Consolas, Liberation Mono, monospace',
  sans: 'Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", "Liberation Sans", Helvetica, Arial, system-ui, sans-serif',
  serif: 'PT Serif, serif',
}

/** @alpha */
export const radius = [0, 1, 3, 6, 9, 12, 21]

/**
 * NOTE: these are identical to Tailwind’s `screens`
 * @alpha
 */
export const screen = [640, 768, 1024, 1280, 1536]

/**
 * The Sanity spacing scale.
 * @alpha
 */
export const space = [0, 4, 8, 12, 20, 32, 52, 84, 136, 220]
